import React from 'react';
import { NotFoundContainer } from './styles';

const NotFound = () => {
  return <NotFoundContainer>잘못된 페이지에 들어왔습니다 🥲</NotFoundContainer>;
};

export default NotFound;
